"use client";
import { Menu,Shield, X } from "lucide-react";
import { useEffect,useRef,useState } from "react";
export default function ShowcaseHeader(){const [open,setOpen]=useState(false),trigger=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!open)return;const onKey=(event:KeyboardEvent)=>{if(event.key==="Escape"){setOpen(false);requestAnimationFrame(()=>trigger.current?.focus())}};document.addEventListener("keydown",onKey);return()=>document.removeEventListener("keydown",onKey)},[open]);return <header className="showcase-header"><a className="brand" href="#top"><span><Shield/></span><b>KEV</b><small>COMMAND CENTER</small></a><nav aria-label="Showcase" className={open?"showcase-nav open":"showcase-nav"}><a href="#intelligence" onClick={()=>setOpen(false)}>Intelligence</a><a href="#capabilities" onClick={()=>setOpen(false)}>Capabilities</a><a href="#methodology" onClick={()=>setOpen(false)}>Methodology</a><a href="#about" onClick={()=>setOpen(false)}>About</a><a className="button small" href="/command-center">Enter Command Center</a></nav><button ref={trigger} className="showcase-menu" aria-label={open?"Close menu":"Open menu"} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></header>}
