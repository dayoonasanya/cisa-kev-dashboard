import { ArrowUpRight } from "lucide-react";
import type { ShowcaseModel } from "@/src/kev/types";
import { buildCommandCenterUrl } from "@/src/showcase/links";
export default function IntelligencePicture({model}:{model:ShowcaseModel}){return <section id="intelligence" className="showcase-section intelligence-picture"><div className="section-heading"><p>01 / CURRENT INTELLIGENCE PICTURE</p><h2>Evidence with operational context.</h2><span>Snapshot as of {model.asOfDate}</span></div><div className="intel-layout"><article className="headline-intel"><span>LEAD ASSESSMENT</span><h3>{model.headline}</h3><p>{model.ransomwareSummary}</p><div className="posture"><i/><b>{model.posture}</b><small>Derived from transparent KEV Action Priority signals</small></div></article><ol className="priority-list">{model.priorityRecords.map((record,index)=><li key={record.cveID}><span>0{index+1}</span><div><b>{record.cveID}</b><strong>{record.vendorProject} · {record.product}</strong><small>{record.vulnerabilityName}</small></div><a href={buildCommandCenterUrl({view:"explorer",cve:record.cveID})} aria-label={`Review ${record.cveID}`}><ArrowUpRight/></a></li>)}</ol></div></section>}
