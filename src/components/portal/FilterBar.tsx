import { RotateCcw } from "lucide-react";
import type { DashboardFilters } from "@/src/kev/types";

export default function FilterBar({filters,vendors,years,onChange,onReset}:{filters:DashboardFilters;vendors:string[];years:string[];onChange:(key:keyof DashboardFilters,value:string)=>void;onReset:()=>void}){return <section className="filter-bar" aria-label="Global intelligence filters"><div className="filter-intro"><span>ACTIVE SCOPE</span><p>Coordinate every metric and chart.</p></div><label>Vendor<select aria-label="Vendor" value={filters.vendor} onChange={(event)=>onChange("vendor",event.target.value)}><option value="">All vendors</option>{vendors.map((vendor)=><option key={vendor}>{vendor}</option>)}</select></label><label>Year added<select aria-label="Year added" value={filters.year} onChange={(event)=>onChange("year",event.target.value)}><option value="">All years</option>{years.map((year)=><option key={year}>{year}</option>)}</select></label><label>Ransomware<select aria-label="Ransomware status" value={filters.ransomware} onChange={(event)=>onChange("ransomware",event.target.value)}><option value="">Known + Unknown</option><option value="Known">Known</option><option value="Unknown">Unknown</option></select></label><button className="reset-button" onClick={onReset}><RotateCcw size={15}/>Reset filters</button></section>}
