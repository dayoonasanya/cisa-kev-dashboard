"use client";
import { AlertTriangle,Building2,Database,Printer } from "lucide-react";
import type { KevDataset,KevRecord } from "@/src/kev/types";
import { deriveBriefing } from "@/src/kev/insights";
import { calculatePriority } from "@/src/kev/priority";
import { MetricCard,SectionHeading } from "@/src/components/portal/common";
import PriorityBadge from "@/src/components/vulnerabilities/PriorityBadge";

export default function BriefingView({records,dataset,asOfDate}:{records:KevRecord[];dataset:KevDataset;asOfDate:string}){
  const brief=deriveBriefing(records,asOfDate);
  return <article className="view-stack briefing-view"><section className="briefing-masthead"><div><span className="eyebrow">EXECUTIVE SECURITY INTELLIGENCE</span><h1>Decision briefing</h1><p>{brief.total.toLocaleString()} catalog entries in the active scope · CISA KEV {dataset.catalogVersion} · {asOfDate}</p></div><button className="print-button" onClick={()=>window.print()}><Printer/> Print briefing</button></section><section className="briefing-lead"><span>HEADLINE</span><h2>{brief.headline}</h2><p>{brief.ransomwareSummary} This portfolio view highlights confirmed exploitation evidence; it does not claim that products are present or vulnerable in your environment.</p></section><section className="metrics-grid briefing-metrics"><MetricCard label="Scoped entries" value={brief.total.toLocaleString()} detail="CISA-confirmed exploitation" icon={Database}/><MetricCard label="Vendors" value={brief.vendors.toLocaleString()} detail="CISA-provided labels" icon={Building2}/><MetricCard label="Ransomware-linked" value={brief.ransomwareCount.toLocaleString()} detail={`${(brief.ransomwareShare*100).toFixed(1)}% of scope`} icon={AlertTriangle} tone="red"/><MetricCard label="Critical priority" value={String(brief.priorityDistribution.Critical)} detail="Portfolio review queue" icon={AlertTriangle} tone="amber"/></section><section className="briefing-grid"><div className="briefing-section"><SectionHeading eyebrow="PRIORITY QUEUE" title="Three records to review first"/><div className="briefing-priorities">{brief.priorityRecords.map((record)=>{const priority=calculatePriority(record,asOfDate);return <article key={record.cveID} data-testid="briefing-priority-record"><PriorityBadge band={priority.band} score={priority.score}/><div><strong>{record.cveID}</strong><h3>{record.vulnerabilityName}</h3><p>{record.vendorProject} · {record.product}</p></div></article>})}</div></div><div className="briefing-section distribution"><SectionHeading eyebrow="ACTIVE SCOPE" title="Priority distribution"/>{Object.entries(brief.priorityDistribution).map(([band,count])=><div key={band}><span>{band}</span><strong>{count}</strong></div>)}</div></section><section className="methodology"><SectionHeading eyebrow="TRANSPARENT MODEL" title="KEV Action Priority methodology" detail="A reproducible portfolio triage aid—not CVSS, EPSS, or an official CISA severity rating."/><div className="method-grid"><article><strong>0 / 40</strong><h3>Ransomware evidence</h3><p>Known campaign use: 40. Unknown: 0; Unknown means unconfirmed, not known-safe.</p></article><article><strong>0–30</strong><h3>Remediation window</h3><p>≤7 days: 30 · 8–14: 24 · 15–21: 18 · 22–30: 12 · 31+: 6.</p></article><article><strong>0–20</strong><h3>Catalog recency</h3><p>≤30 days: 20 · ≤90: 15 · ≤180: 10 · ≤365: 5 · older: 0.</p></article><article><strong>0–10</strong><h3>Due status</h3><p>Overdue: 10 · due within 30 days: 6 · later: 0.</p></article></div><div className="method-bands"><b>Priority bands:</b> Critical 80–100 · High 60–79 · Elevated 40–59 · Tracked 0–39. Dates are calculated at UTC midnight against snapshot date {asOfDate}; invalid dates contribute zero points.</div><p className="method-caveat"><b>Interpretation:</b> Use the score to order investigation, then validate asset presence, product version, exposure, compensating controls, and business criticality in your own environment.</p></section><footer className="briefing-source">Source: <a href={dataset.sourceUrl}>{dataset.sourceUrl}</a> · Retrieved {dataset.retrievedAt.slice(0,10)} · CISA KEV data is CC0 1.0.</footer></article>;
}
